apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: yandex-lockbox
  namespace: minecraft
spec:
  provider:
    yandexlockbox:
      auth:
        authorizedKeySecretRef:
          name: yc-auth
          key: authorized-key
